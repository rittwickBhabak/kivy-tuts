<MyLayout>

    # Either use below 6 lines or 
    # change it by Window.clearcolor in build method 
    # if uses both, the former will overwrite the later one 
    canvas.before:
        Color:
            rgba: (1, 1, 1, 1)
        Rectangle:
            pos: self.pos 
            size: self.size 

    BoxLayout:
        orientation: "vertical"
        size: root.width, root.height
        padding: 50
        spacing: 50

      
        Button:
            text: "Hello World!"

        Button:
            text: "Hello Rittwick!"